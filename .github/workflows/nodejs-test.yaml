name: Test

on:
  workflow_call:
    inputs:
      test-command:
        required: false
        type: string
        default: 'npm run test'
      node-version:
        required: false
        type: string
        default: 'latest'
      npm-version:
        required: false
        type: string
        default: 'latest'
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Install dependencies
        uses: dvsa/.github/.github/actions/install-deps@v6
        with:
          node-version: ${{ inputs.node-version }}
          npm-version: ${{ inputs.npm-version }}
      - name: Test
        run: ${{ inputs.test-command }}
